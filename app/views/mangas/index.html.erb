<h1>Liste des mangas</h1>
<!--
<table>
	<thead>
		<th>Manga</th>
		<th>Titre</th>
		<th>Titre traduit</th>
		<th>Date de publication</th>
		<th>Numéro de Tome</th>
		<th>Genre</th>
		<th>Auteur</th>

	</thead>
	<tbody>
		
		<% @mangas.each do |manga| %>
			<%@genre = Genre.find(manga.genre_id) %>
			<%@image = Image.find(manga.image_id) %>
			<%@auteur = Auteur.find(manga.auteur_id) %>

			
			<tr>
				<td><%= image_tag @image.url, alt: @image.name %> 				
				<td><%= manga.title %>
				<td><%= manga.title_traduction %>
				<td><%= manga.realise_date %>
				<td><%= manga.tome_number %> tomes 
				<td><%= link_to @genre.name , @genre %>
				<td><%= link_to @auteur.first_name , @auteur %>
				<td><%= link_to 'Afficher', manga %>
				<td><%= link_to 'Supprimer', manga, method: :delete, data: {confirm: 'Etes-vous sûr ?'} %>
			</tr>

		<% end %>
	</tbody>
</table>

-->

<tbody>
		
<% @mangas.each do |manga| %>
			<%@genre = Genre.find(manga.genre_id) %>
			<%@image = Image.find(manga.image_id) %>
			<%@auteur = Auteur.find(manga.auteur_id) %>

			
		
		<div class="manga">
			<p>
				<%= image_tag @image.url, alt: @image.name , style:"height: 7%; width: 7%;" %> 	
				<%= manga.title %>
				<%= manga.title_traduction %>
				<%= manga.realise_date %>
				<%= manga.tome_number %> tomes 
				<%= link_to @genre.name , @genre %>
				<%= link_to @auteur.first_name , @auteur %>
				<%= link_to 'Afficher', manga %>
				<%= link_to 'Supprimer', manga, method: :delete, data: {confirm: 'Etes-vous sûr ?'} %>
			</p>
		</div>

		<% end %>
	</tbody>